import React, { FC } from 'react';

interface Props {
    countries: any,
    propertyTypes: any,
    regions: any,
    cities: any
}

const SearchBox: FC<Props> = (props) => {

    return(
        <></>
    )
}